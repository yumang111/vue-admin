import installment from './installment.component.vue'

const installmentRoutes = [
  {
    path: 'installment',
    name: 'installment',
    component: installment
  }
]
export default installmentRoutes
